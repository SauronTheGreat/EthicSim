<%= form_for(@facilitator_group) do |f| %>
  <% if @facilitator_group.errors.any? %>
	<div id="error_explanation">
	  <h2><%= pluralize(@facilitator_group.errors.count, "error") %> prohibited this facilitator_group from being
		saved:</h2>

	  <ul>
		<% @facilitator_group.errors.full_messages.each do |msg| %>
		  <li><%= msg %></li>
		<% end %>
	  </ul>
	</div>
  <% end %>

  <table >
	<tr>
	  <td style="text-align: left;">

		<%= f.label "Client Name" %></td>
	  <td style="text-align: left;"><%= f.select :client_id, Client.all.map { |client| [client.name, client.id] } %></td>


	</tr>


	<tr>

	  <td style="text-align: left;">


		<%= f.label :name %></td>
	  <td style="text-align: left;"><%= f.text_field :name %>

	  </td>
	</tr>
  </table>
  <br>
  <br>
  <br>


  <div class="actions">
	<%= f.submit %>
  </div>
<% end %>
